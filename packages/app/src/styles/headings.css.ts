import { css } from "lit";

const styles = css`
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-family: var(--font-display, system-ui);
    font-weight: 600;
    line-height: 1.2;
    margin: 0 0 0.5em;
  }
`;

export default { styles };
